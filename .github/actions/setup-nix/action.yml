name: "Setup Nix"
description: "Sets up Nix for a job"
runs:
  using: "composite"
  steps:
    - uses: DeterminateSystems/nix-installer-action@main
    - uses: DeterminateSystems/magic-nix-cache-action@main
      with:
        use-flakehub: false
        diagnostic-endpoint: ''
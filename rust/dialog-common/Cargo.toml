[package]
name = "dialog-common"
description = "Shared, stand-alone helpers"
edition = "2024"
version.workspace = true
authors.workspace = true
license.workspace = true

[features]
default = []
# Testing utilities (Provision, Provider traits, EnvGuard)
helpers = ["dep:anyhow", "dep:serde", "dep:serde_json", "dep:dialog-macros", "dep:async-trait"]

[dependencies]
blake3 = { workspace = true }
anyhow = { workspace = true, optional = true }
serde = { workspace = true, features = ["derive"], optional = true }
serde_json = { version = "1.0", optional = true }
dialog-macros = { workspace = true, optional = true }
async-trait = { workspace = true, optional = true }

[dev-dependencies]
tokio = { workspace = true, features = ["rt", "macros"] }
wasm-bindgen = { workspace = true }
wasm-bindgen-test = { workspace = true }

[lints.rust]
# These cfgs are used by the dialog_common::test proc macro for wasm testing
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(dialog_test_wasm)", "cfg(dialog_test_wasm_inner)"] }
